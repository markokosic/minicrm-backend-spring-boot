meta {
  name: /login
  type: http
  seq: 2
}

post {
  url: {{API_URL}}/auth/login
  body: json
  auth: none
}

headers {
  ~Accept: application/json
}

body:json {
  {
    "email": "hi@markokosic.com",
    "password": "abc@123!"
  }
}
